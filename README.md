# LXC mount host directory

A very thin wrapper around command `lxc config device add ...`

The usage output ( e.g. `./lxc-host-dir.sh --help`) is generated by <https://github.com/docopt/docopts>.

Requires docopts to be installed on your system.
